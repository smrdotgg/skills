---
name: explain-print
description: Use when the user asks for a printable explanation of anything — a code change, PR, branch, subsystem, architecture, data flow, algorithm, tool, or concept. Produces printable HTML for reading away from a screen.
---

# Explain for print

Read the `explain` skill and follow it fully — subject exploration, document structure, writing, and completion checks — producing the paper-mode output:

- Before writing, read the `print-mode-html` skill and follow it for all HTML, typography, print layout, file location, completion checks, and delivery. This replaces the screen-mode delivery of `create-report`.
